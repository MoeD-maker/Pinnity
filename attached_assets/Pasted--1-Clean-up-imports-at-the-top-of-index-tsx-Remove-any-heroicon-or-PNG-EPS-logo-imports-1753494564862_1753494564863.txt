### 1) Clean up imports at the top of `index.tsx`
// 🔥 Remove any heroicon or PNG/EPS logo imports
import FullLogo     from "assets/logo-full.svg";
import PinIcon      from "assets/logo-icon.svg";
// 👇 Use your existing bullet icon for list items (e.g. a map‑marker or dollar icon)
// import BulletIcon from "assets/bullet-icon.svg"; // or keep your current import

### 2) Wrapper for two‑panel layout
```jsx
<div className="md:flex h-screen">
  {/* LEFT PANEL */}
  <div className="md:w-1/2 bg-white flex flex-col">
    {/* Header with ONLY full logo */}
    <header className="flex justify-center py-8">
      <FullLogo className="h-20 w-auto" aria-label="Pinnity logo" />
    </header>
    {/* …your existing login form below… */}
  </div>

  {/* RIGHT PANEL */}
  <div className="md:w-1/2 relative bg-teal-800 p-12 flex items-center justify-center overflow-hidden">
    {/* BACKGROUND PIN */}
    <PinIcon
      className="absolute inset-0 m-auto h-32 w-auto opacity-10"
      aria-hidden="true"
    />

    {/* CONTENT ON TOP */}
    <div className="relative z-10 max-w-lg space-y-6">
      <h2 className="text-4xl font-bold text-white">Discover Local</h2>
      <p className="text-white/80">
        Join the Pinnity community to discover and connect with amazing local businesses and experiences in your area.
      </p>
      <ul className="space-y-4">
        <li className="flex items-start bg-teal-700 rounded-lg p-4">
          <BulletIcon className="h-6 text-amber-400 flex-shrink-0" />
          <div className="ml-3">
            <h3 className="font-semibold text-white">Explore Your Neighborhood</h3>
            <p className="text-white/80 text-sm">Find hidden gems and local favorites right around the corner.</p>
          </div>
        </li>
        {/* …repeat for Support Local Businesses & Win‑Win Discounts… */}
      </ul>
    </div>
  </div>
</div>
Key points

Left header imports only FullLogo and displays it at h-20 (≈80 px), centered. No stray <PinIcon> or text.

Right panel imports only PinIcon and places a single large, faded (opacity-10) icon at h-32. It sits behind your marketing copy thanks to absolute/inset-0 m-auto and z‑10 on the content.

Bullet list still uses your regular bullet icons (amber), not the pin.

Uses Tailwind’s md:flex for side‑by‑side on desktop, stacking below md.