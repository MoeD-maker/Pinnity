### 1) Imports  
```tsx
// Full wordmark logo (icon + “Pinnity Discover Local”)
import FullLogo from "assets/logo-full.svg";
// Standalone pin‑heart icon for background
import PinIcon  from "assets/logo-icon.svg";
2) Left panel header
Replace whatever you have in your <header> with only the full logo SVG—no extra text or pin:

tsx
Copy
Edit
<header className="flex flex-col items-center py-8 bg-white">
  <FullLogo className="h-20 w-auto mb-6" aria-label="Pinnity logo" />
</header>
h-20 → ~80 px tall, crisp wordmark.

Removes any duplicated <h1>/<p> or stray icons.

3) Right panel background pin
Embed a moderately sized, faded pin behind your marketing text:

tsx
Copy
Edit
<aside className="relative flex-1 bg-teal-800 p-12 flex flex-col justify-center overflow-hidden">
  {/* background pin, centered */}
  <PinIcon
    className="absolute inset-0 m-auto h-32 w-auto opacity-10"
    aria-hidden="true"
  />

  {/* actual content on top */}
  <div className="relative z-10 max-w-lg mx-auto space-y-6">
    <h2 className="text-4xl font-bold text-white">Discover Local</h2>
    <p className="text-white/80">
      Join the Pinnity community to discover and connect with amazing local businesses and experiences in your area.
    </p>
    {/* …your bullet list here, amber icons only inside list items… */}
  </div>
</aside>
h-32 → ~128 px tall, not overwhelming.

opacity-10 keeps it subtle.

No white box—pin is transparent PNG/SVG.

4) Layout wrapper & responsive
html
Copy
Edit
<div className="md:flex">
  <div className="md:w-1/2 bg-white">…left panel…</div>
  <div className="md:w-1/2">…right panel…</div>
</div>
Side‑by‑side on md+, stacked on mobile.

If you ever need to hide the background pin on small screens: add hidden sm:block to the <PinIcon>.

Colors & classes

Teal: bg-teal-800 (#007C72)

White copy: text-white / text-white/80

No other logo imports—this forces the true full‑logo on the left and the correct icon at a controlled size behind the right panel.