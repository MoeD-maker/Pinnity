### 1. Imports
// ONLY import the standalone pin icon SVG
import PinIcon from "assets/logo-icon.svg";

### 2. Left Panel Header (above the form)
<header className="flex flex-col items-center py-8 bg-white">
  <PinIcon className="h-24 w-auto mb-3" aria-hidden="true" />
  <h1 className="text-3xl font-semibold text-gray-800">Pinnity</h1>
  <p className="mt-1 text-teal-700 italic">Discover Local</p>
</header>

### 3. Right Panel (“marketing”) with centered, faded background pin
<aside className="relative flex-1 bg-teal-800 p-12 flex flex-col justify-center overflow-hidden">
  {/* BACKGROUND PIN */}
  <PinIcon
    className="absolute inset-0 m-auto h-40 w-auto opacity-10"
    aria-hidden="true"
  />

  {/* CONTENT (on top of pin) */}
  <div className="relative z-10 space-y-8 max-w-lg">
    <h2 className="text-4xl font-bold text-white">Discover Local</h2>
    <p className="mt-2 text-white/80">
      Join the Pinnity community to discover and connect with amazing local businesses and experiences in your area.
    </p>
    <ul className="mt-6 space-y-4">
      <li className="flex items-start bg-teal-700 rounded-lg p-4">
        <span className="flex-shrink-0 text-amber-400">
          {/* reuse PinIcon at smaller size, or swap in a bullet icon */}
          <PinIcon className="h-6 w-auto" />
        </span>
        <div className="ml-3">
          <h3 className="font-semibold text-white">Explore Your Neighborhood</h3>
          <p className="text-white/80 text-sm">Find hidden gems and local favorites right around the corner.</p>
        </div>
      </li>
      {/* ...repeat for Support Local Businesses & Win‑Win Discounts */}
    </ul>
  </div>
</aside>

### 4. Responsive
- Wrap panels in a parent `<div className="md:flex">`.
- On `md:` show side‑by‑side; below `md` stack `<header>`, form, then `<aside>`.
- Use Tailwind’s `hidden md:block` / `block md:hidden` as needed.

### 5. Colours & Typography
- **Teal** panels: `bg-teal-800` (#007C72)  
- **Text**: dark grey `text-gray-800` (#334155) & white/80% for copy on teal  
- **Accents**: amber `#FFC107` for bullet icons only  
- **PinIcon** on teal background: always `opacity-10` & large `h-40`
