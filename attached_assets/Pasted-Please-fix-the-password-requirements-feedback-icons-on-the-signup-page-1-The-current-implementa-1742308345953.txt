Please fix the password requirements feedback icons on the signup page:

1. The current implementation shows X marks next to each password requirement, but they don't update when conditions are met.

2. Update the password requirements list to show dynamic icons:
   - Change each X icon to a check mark (✓) when the specific requirement is met
   - Keep the X icon when the requirement is not met
   - Use text-red-500 for unfulfilled requirements and text-green-500 for fulfilled ones

3. In the PasswordStrengthIndicator component or related component that displays these requirements:
   - Add logic to check each requirement separately (length, uppercase, number, special char)
   - Update the icon and color based on the validation state
   - Sample implementation:
     
     ```jsx
     const requirements = [
       { 
         text: "At least 8 characters", 
         met: password.length >= 8,
       },
       { 
         text: "At least one uppercase letter", 
         met: /[A-Z]/.test(password),
       },
       { 
         text: "At least one number", 
         met: /[0-9]/.test(password),
       },
       { 
         text: "At least one special character", 
         met: /[^A-Za-z0-9]/.test(password),
       }
     ];
     
     return (
       <div className="mt-2">
         <p className="text-sm font-medium mb-1">Password requirements:</p>
         <ul className="text-xs space-y-1">
           {requirements.map((req, index) => (
             <li key={index} className={`flex items-center ${req.met ? 'text-green-500' : 'text-red-500'}`}>
               {req.met ? 
                 <CheckIcon className="h-3 w-3 mr-2" /> : 
                 <XIcon className="h-3 w-3 mr-2" />
               }
               {req.text}
             </li>
           ))}
         </ul>
       </div>
     );
     ```

4. Ensure the validation happens in real-time as the user types by connecting this to the existing password change handler.